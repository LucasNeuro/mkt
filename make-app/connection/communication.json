{
  "url": "{{trim(parameters.apiUrl)}}/instancia",
  "method": "GET",
  "headers": {
    "Accept": "application/json"
  },
  "response": {
    "valid": {
      "condition": "{{statusCode == 200}}"
    },
    "error": {
      "message": "[{{statusCode}}] {{ifempty(body.detail, ifempty(body.erro, 'Falha ao conectar. Abra a URL no navegador antes (Render free dorme).'))}}"
    },
    "metadata": {
      "type": "text",
      "value": "{{body.nome}}"
    }
  }
}
