{
  "url": "/enviar",
  "method": "POST",
  "body": {
    "text": "{{parameters.text}}",
    "number": "{{ifempty(parameters.number, undefined)}}",
    "group_jid": "{{ifempty(parameters.group_jid, undefined)}}",
    "mentions": "{{ifempty(parameters.mentions, undefined)}}",
    "delay": "{{parameters.delay}}",
    "async": "{{parameters.async}}"
  },
  "response": {
    "output": "{{body}}"
  }
}
